<div class="formulaire"> 
<%= form_for :contact do |f| %>

   	<div class="contact-champ">
		<%= f.label :nom %>
		<%= f.text_field :nom %>
	</div>

	<div class="contact-champ">
		<%= f.label :prenom %>
		<%= f.text_field :prenom %>
	</div>

	<div class="contact-champ">
		<%= f.label :mail %>
		<%= f.text_field :mail %>
	</div>	

	<div class="contact-champ">
		<%= f.text_area :avis, placeholder: "Votre avis nous intéresse..." %>
	</div>	

    <div class="envoyer">
		<%= link_to (image_tag('home.png', title:"page d'accueil")), root_path, { :class=>"retour" } %>
		<%= f.submit "Submit", :type => :image, :src => image_path("envoyer1.png"), :class => "envoyer_img" %>
    </div>
<% end %>
</div>
